# gitleaks configuration — abap_vibe_coding
title = "abap_vibe_coding gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allowlist for known false positives"
regexes = [
  'your[_-]?(api[_-]?)?key[_-]?here',
  'your[_-]?secret[_-]?here',
  'replace[_-]?with[_-]?actual',
  'example[_\-.]com',
  'gitleaks:allow',
]
paths = [
  '\.env\.sample',
  'CHANGELOG\.md',
  '.*_test\.(js|ts|py|go|rs)$',
  '.*\.test\.(js|ts)$',
  'memory/.*',
  'scratch/.*\.abap$',
]

# ABAP variable name patterns (false positives: iv_key, ls_key, key1, etc. are ABAP identifiers)
[[rules]]
id = "abap-variable-false-positive"
description = "Suppress ABAP variable assignments that trigger generic key rules"
regex = '''(iv_key|ls_key|lv_key|key1|key2|lv_pw)\s*=\s*[a-z_\-\[\]0-9]+'''
allowlist.regexTarget = "match"
allowlist.regexes = ['.*']
