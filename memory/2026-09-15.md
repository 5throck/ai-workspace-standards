## Session Summary
05:30 local runner check — nightly run for 09-14 MISSING (schedule skip), but ready manual tickets: 0 — no actionable work existed, no batch performed

05:30 check: nightly run 34778475588 (09-13T19:41 UTC) is the latest; no run created for the 09-14T17:30 UTC schedule (3h past schedule as of check). Ready manual tickets: none (board: 136 done / 0 open). Doctor: clean. Direct batch: not performed — nothing to process. Next meaningful nightly fires when new ready tickets appear (e.g. the pending T-026/027-adjacent CI hardening follow-ups or audit findings).

## PM Tier Alignment (user decision)
- Decision 1: AGENTS.md PM tier documented as **Medium** (was High) — aligns SSOT with the L1/L2 inheritance chain (`templates/common/agents/pm.md` defines medium; all 13 variants inherit it). Updated §1 Agent Roster row and §4.1 Subagent Roster row. Note: L0 `agents/pm.md` frontmatter remains `high` — user scoped the change to AGENTS.md only; residual L0(high) vs SSOT(Medium) tension flagged for follow-up.
- Decision 2: L1 pm.md missing `codex` tier key judged an **omission** — added `codex: medium` (matching L0's key order and L1's medium values); bumped last_updated to 2026-09-15.
- Finding: the codex key is absent from **all** L1 agent tier blocks (`templates/common/agents/*.md`), not just pm.md — systematic pattern, follow-up decision pending on whether to extend codex keys to the other L1 agents.
- Verification: `bun scripts/audit.ts` all checks passed (incl. pm.md L0→L1→L2 alignment). Landing via /sync pending (E3-class trivial change).

## Follow-up: PM tier medium rollout + gate de-hardcoding (user decision)
- `scripts/hooks/agent-model-gate.ts` refactored (1.0.0 → 1.1.0): hardcoded `AGENT_TIERS` map removed; agent→tier now read at runtime from `docs/workspace-schema.json` `agent_tiers` (SSOT), Claude alias derived from `models.claude` registry ID (`claude-<alias>-<ver>`). Fail-open policy on unreadable/incomplete schema (WARN to stderr, dispatch allowed) — gating must not brick Agent calls; schema corruption stays the job of validate-model-registry.ts / audit. Tested 6 hook-input paths (pm ask/Medium-sonnet, explicit-model allow, non-tiered allow, architect ask/High-opus, invalid JSON allow, missing-schema fail-open) — all pass.
- `docs/workspace-schema.json`: `agent_tiers.pm` high → **medium** (single edit now drives the gate automatically).
- `agents/pm.md` (L0): tier high → **medium** across all 5 platforms, model comments updated to medium-tier registry IDs (claude-sonnet-5-0 / gemini-3.8-flash / gpt-5.6-terra); last_updated 2026-09-15.
- `templates/common/agents/i18n-specialist.md`: added missing `codex: medium # gpt-5.6-terra` (was the only other L1 agent with a tier block); lifecycle last_updated 2026-09-15.
- `scripts/SCRIPTS.md`: agent-model-gate.ts registry version 1.0.0 → 1.1.0 (Check A sync).
- Validation: `bun scripts/validate-model-registry.ts` ✓, `bun scripts/audit.ts` all checks ✓. Landing via /sync pending.

## Agent Tier Drift Check (Check F) — user-requested computed governance
- User insight: the PM tier change did not propagate simultaneously to AGENTS.md / workspace-schema.json — asked whether the lifecycle-manager skill should COMPUTE drift instead of relying on manual sync. Plan approved (plan mode); design doc: docs/designs/2026-09-15-agent-tier-drift-check-design.md (spec 2026-09-15-agent-tier-drift-check, registered).
- Investigation found 6 tier surfaces with zero cross-validation (L0 frontmatter, AGENTS.md §1/§4.1 rosters, schema agent_tiers, lifecycle records, L1 templates). validate-templates WS-01 Check 4 only WARNs on complex tier blocks (all 8 agents). Live drift existed: docs/lifecycle/agents/pm.md still said `**Tier**: high` + "High-tier (claude-opus-5-0...)" prose.
- Implemented `runCheckF()` in lifecycle-sync-audit.ts (v1.9.0 → 1.10.0, L0+L1 mirrors via propagate-to-templates lockstep): agent_tiers = SSOT; compares L0 frontmatter (5 platforms, uniform), L1 common-template agents (SSOT match when registered — pm.md; completeness/uniformity otherwise — i18n-specialist.md), AGENTS.md roster Tier cells (table-header-scoped parser after fixing a §5.3-example-plan false positive), lifecycle `**Tier**` fields. Detection-only, error-level; checksRun 7→8.
- Remediated live drift: pm/docs-writer/automation-engineer/scaffolding-expert lifecycle records normalized to plain tier values; AGENTS.md `**Medium**` bold cells → `Medium` (§1:37, §4.1:355); agent-lifecycle-manager SKILL.md frontmatter template 3→5 platforms (v1.1.0; SKILLS.md registry + lifecycle record synced); schema `_notes.variant_agents` stale variant.json claim corrected.
- Procedure wired in: SKILL.md gains "Changing an Agent's Tier" section (schema first → frontmatter → rosters → record, Check F must pass); agents/lifecycle-manager.md Drift Reporting + State Monitoring now reference Check F.
- Verification: lifecycle-sync-audit 8 checks clean; negative test (schema pm→high) reported exactly 5 findings across all 4 surfaces then reverted clean; `bun scripts/audit.ts` all checks pass. Landing via /sync pending.

## Root co-* Stray Cleanup + Workspace-Root Target Guards (user-requested inspection → fix)
- User noticed 13 `co-*` dirs + `common/` + empty `procedures/` at the workspace root and asked for an inspection plus whether `upgrade-project` or `project-resync` caused them.
- Root cause confirmed: **`upgrade-project` run targeted the workspace ROOT on 2026-09-12 19:27** — the same root-upgrade incident already recorded in `memory/2026-09-12.md:2148` (recovery commit `6a4ece8c`). `project-resync` exonerated (ran 22:34/22:57 against `Projects/co-*`, PRs #908/#909). The 15 strays are a byte-verified snapshot of `templates/` at commit `b163891c` (only diffs: gitignored `co-design/playground` build outputs, identical to today's templates copies); no `.git`, no user content, mtimes frozen at 19:27:14–15. The 9/13 disposal missed them because `.gitignore:10` (`/*/`) hides root dirs from `git status`/`git clean`.
- Cleanup: all 15 verified safe → archived to `../root-strays-20260912.tar.gz` (3647 files, outside the repo) → deleted. `Projects/` untouched. Root-only names (co-design/co-hr/co-news/co-work) are template renders, not lost projects.
- Guards: `upgrade-project.ts` 1.26.0 → 1.27.0 (rejects workspace-ROOT `<project-path>`, warns outside `Projects/`); `new-project.ts` 1.15.1 → 1.16.0 (bare names scaffold to `Projects/<name>`; path-like names stay workspace-relative for `test-new-project.ts`; workspace escape rejected); `tests/reflect-skill-graph-to-project.ts` root-target rejection.
- Tests: new `tests/unit/project-target-guards.test.ts` (4 spawn tests, all pass); existing related suites (upgrade-policy, upgrade-tree-sync, new-project-rollback, co-safety completeness, scripts-md-l2-filtering) 53 pass / 0 fail.
- Docs: SCRIPTS.md 2 registry rows + `new-project.ts` section updated; `skills/promote-variant/SKILL.md` + `skills/simulate-pipeline/SKILL.md` path guidance now matches actual scaffold behavior.
- Spec: docs/designs/2026-09-15-root-stray-cleanup-design.md registered (spec 2026-09-15-root-stray-cleanup, source manual, status implemented). Landing via /sync pending.

## Skills Used
### sync
- Observed Symptom: (none — deliberate pipeline run to finalize the root-stray cleanup + workspace-root target guards, and to land the two earlier pending governance sessions (PM tier medium + Check F drift detection) sharing the working tree)
- Evidence: this file; `docs/designs/2026-09-15-root-stray-cleanup-design.md`; dev-sync pipeline output and PR body


---

## Session Summary
fix(scripts): remove root-upgrade strays, add workspace-root target guards

## Changes
- `AGENTS.md` — modified
- `agents/lifecycle-manager.md` — modified
- `agents/pm.md` — modified
- `docs/designs/2026-09-15-agent-tier-drift-check-design.md` — modified
- `docs/designs/2026-09-15-root-stray-cleanup-design.md` — modified
- `docs/lifecycle/agents/automation-engineer.md` — modified
- `docs/lifecycle/agents/docs-writer.md` — modified
- `docs/lifecycle/agents/pm.md` — modified
- `docs/lifecycle/agents/scaffolding-expert.md` — modified
- `docs/lifecycle/skills/agent-lifecycle-manager.md` — modified
- `docs/specs/registry.json` — modified
- `docs/workspace-schema.json` — modified
- `memory/2026-09-15.md` — modified
- `scripts/SCRIPTS.md` — modified
- `scripts/hooks/agent-model-gate.ts` — modified
- `scripts/lifecycle-sync-audit.ts` — modified
- `scripts/new-project.ts` — modified
- `scripts/upgrade-project.ts` — modified
- `skills/SKILLS.md` — modified
- `skills/agent-lifecycle-manager/SKILL.md` — modified
- `skills/promote-variant/SKILL.md` — modified
- `skills/simulate-pipeline/SKILL.md` — modified
- `templates/common/agents/i18n-specialist.md` — modified
- `templates/common/agents/pm.md` — modified
- `templates/common/scripts/SCRIPTS.md` — modified
- `templates/common/scripts/lifecycle-sync-audit.ts` — modified
- `templates/common/skills/agent-lifecycle-manager/SKILL.md` — modified
- `tests/reflect-skill-graph-to-project.ts` — modified
- `tests/unit/project-target-guards.test.ts` — modified

## Decisions
- None

## Open Issues
- None
