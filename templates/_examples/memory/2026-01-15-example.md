# Session Log — 2026-01-15

## Summary
Implemented user authentication endpoints (register + login) per the approved plan in
`docs/adr/0002-auth-model.md`. All acceptance criteria passed. PR #4 opened.

## Changes Made
- `src/models/user.py` — created User model (id, email, hashed_password, created_at)
- `src/routes/auth.py` — added POST /register and POST /login endpoints
- `src/config.py` — added JWT_SECRET env var (also added to .env.sample)
- `tests/test_auth.py` — added 8 tests covering register, login, duplicate email, invalid creds
- `docs/adr/0002-auth-model.md` — status updated to Accepted

## Decisions
- Used bcrypt for password hashing (vs argon2) — simpler dependency, sufficient for current scale
- JWT expiry set to 7 days — can be made configurable later if needed

## Open Items
- [ ] Add password reset flow (deferred to next sprint)
- [ ] Review JWT_SECRET rotation strategy before production deploy
