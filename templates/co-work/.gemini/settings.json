{
  "_comment": "Variant-specific overrides vs L1 (templates/common): unpinned codegraph version (-y) for auto-updates, PostToolUse lifecycle check hook added. Intentionally minimal — terminal.executionPolicy, mcp.toolApproval, and terminal.denyList are omitted to rely on Gemini CLI defaults for co-work projects.",
  "mcpServers": {
    "codegraph": {
      "command": "npx",
      "args": [
        "-y",
        "@colbymchenry/codegraph",
        "serve"
      ]
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "git config core.hooksPath .githooks",
        "statusMessage": "Configuring git hooks..."
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bun scripts/hooks/post-write-lifecycle-check.ts",
            "statusMessage": "Running post-edit lifecycle check..."
          }
        ]
      }
    ]
  },
  "artifact.reviewPolicy": "Auto"
}
