---
id: DEC-20260825-02
date: 2026-08-25
agent: pm
decision: Route the samm-maturity skill commit through the /sync pipeline after the direct-commit denial; never bypass hooks.
alternatives: (1) Direct `git commit` — attempted first and blocked by the pre-commit hook ("Direct git commits are restricted; use /sync"). (2) `--no-verify` — forbidden workspace-wide; it would bypass secret scanning and all quality gates. (3) Leave the file uncommitted — rejected: dev-sync's `git add -A` would sweep it into any future unrelated PR anyway.
evidence_refs: [EV-20260825-002]
rules_applied: []
skills_used: [sync]
knowledge_refs: [docs/adr/0061-decision-record-standard.md, .agents/skills/sync/SKILL.md]
status: accepted
---

## Ruling

Committing a completed but untracked variant skill (`templates/co-security/skills/samm-maturity/SKILL.md`)
was denied by the TS pre-commit hook. The sanctioned path was taken end-to-end:
CHANGELOG `[Unreleased]` entry → PR body file → `bun scripts/dev-sync.ts --body-file …`
→ branch `pr/20260824-160331-…` → push → **PR #651** (merged). The hook's refusal is the
system working: one pathway, full gates.

## Consequences

- "커밋해 줘" from a user maps to `/sync`, never to raw git plumbing.
- Untracked work-in-progress in a repo with `add -A` pipelines is latent sweep risk:
  finish or quarantine it before running any sync.
